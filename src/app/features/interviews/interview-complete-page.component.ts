import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { MatButtonModule } from '@angular/material/button';
import { ActivatedRoute, Router, RouterLink } from '@angular/router';
import { finalize } from 'rxjs';
import { NotificationService } from '../../core/services/notification.service';
import { InterviewApiService } from './interview-api.service';
@Component({standalone:true,selector:'app-interview-complete-page',imports:[MatButtonModule,RouterLink],template:`<section class="complete-card"><div class="check">✓</div><h1>Interview completed</h1><p>All five answers were saved. Generate your AI evaluation to see evidence-based scores and question feedback.</p>@if(error()){<div class="error">{{error()}}</div>}<button mat-flat-button color="primary" [disabled]="loading()" (click)="generate()">{{loading()?'Evaluating your answers…':'Generate interview results'}}</button><a mat-button routerLink="/app/interviews/setup">Create another interview</a></section>`,styles:['.complete-card{max-width:720px;margin:80px auto;padding:60px 36px;border:1px solid var(--border);border-radius:18px;background:#fff;text-align:center}.check{display:grid;width:80px;height:80px;margin:0 auto 22px;place-items:center;border-radius:50%;background:#e7faf4;color:#009b73;font-size:36px}.complete-card h1{margin:0 0 12px}.complete-card p{margin:0 auto 28px;max-width:560px;color:var(--text-secondary);line-height:1.6}.complete-card button,.complete-card a{margin:4px}.error{margin:0 auto 20px;padding:12px;border-radius:8px;background:#fff1f0;color:#b42318}'],changeDetection:ChangeDetectionStrategy.OnPush})
export class InterviewCompletePageComponent{private readonly route=inject(ActivatedRoute);private readonly router=inject(Router);private readonly api=inject(InterviewApiService);private readonly notifications=inject(NotificationService);readonly id=this.route.snapshot.paramMap.get('id')!;readonly loading=signal(false);readonly error=signal('');generate():void{if(this.loading())return;this.loading.set(true);this.error.set('');this.api.evaluate(this.id).pipe(finalize(()=>this.loading.set(false))).subscribe({next:()=>void this.router.navigate(['/app/interviews',this.id,'results']),error:e=>{this.error.set('Evaluation could not be generated. Your answers are safe; please try again.');this.notifications.error(e,'Unable to generate interview results.');}});}}
